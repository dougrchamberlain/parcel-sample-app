export default function(){

        service = {};
        service.userStories = [];



        service.initialize = function(numPlayers){
            //
            service.userStories.push(userStory) //config for that story

        }

        service.status = function(){
            return {
                currentStory: story,
                merged: true
            }
        }

        return service;

}